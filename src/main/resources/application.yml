server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://aidatabase.ch6z9mmu9bas.ap-northeast-2.rds.amazonaws.com:3306/aiservice?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username:
    password:

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true